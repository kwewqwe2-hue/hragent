# SaaS application

This directory contains the Spring Boot backend, Vue administration frontend,
Docker Compose definition, import templates, and SaaS product documentation.

Use the repository-level scripts and README for setup. Do not run this Compose
project before `n8nwork`, because it joins the external `n8nwork_default`
network used by the local agent.
