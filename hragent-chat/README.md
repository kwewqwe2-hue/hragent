# AI web client

Desktop-oriented Vue client for the same n8n agent used by DingTalk. Users log
in with their SaaS account; the SaaS backend signs the trusted identity passed
to n8n. Conversation history in browser storage is not authoritative business
data.

The Docker URL is `http://localhost:5174`. See the repository README for setup.
